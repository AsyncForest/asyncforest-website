**Tag** — a sans chip for filters, categories, or selectable options (service areas, topics). Larger and softer than Badge; can be clicked, selected, or removed.

```jsx
<Tag selected>Architecture review</Tag>
<Tag onRemove={() => {}}>AWS</Tag>
```

Use `selected` for filter state and `onRemove` for removable input chips.
