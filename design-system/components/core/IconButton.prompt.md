**IconButton** — square control for a single icon (menu, close, arrows). Pass a Lucide/SVG node as children and always set an accessible `label`.

```jsx
<IconButton label="Menu" variant="ghost"><Menu size={20} /></IconButton>
```

Variants: `primary | secondary | ghost`. Sizes `sm | md | lg`.
