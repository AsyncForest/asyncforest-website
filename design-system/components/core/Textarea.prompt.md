**Textarea** — multi-line text field for longer input (project context, message). Matches Input styling.

```jsx
<Field label="What would you like reviewed?">
  <Textarea rows={5} placeholder="A few sentences on the system and your concern." />
</Field>
```
