---
import Logo from './Logo.astro';
const { pathname } = Astro.url;
const links = [
  { href: '/services/', label: 'Services' },
  { href: '/how-it-works/', label: 'How it works' },
  { href: '/about/', label: 'About' },
  { href: '/writing/', label: 'Writing' },
];
const isActive = (href: string) => pathname.startsWith(href);
---

<header class="header">
  <div class="container header__inner">
    <a href="/" class="header__home" aria-label="Async Forest Advisory — home">
      <Logo size={26} />
    </a>
    <nav class="header__nav" aria-label="Main">
      {links.map((l) => (
        <a href={l.href} class:list={['header__link', { 'is-active': isActive(l.href) }]}>
          {l.label}
        </a>
      ))}
      <a href="/contact/" class="btn btn--primary header__cta">Start a conversation</a>
    </nav>
  </div>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 10;
    background: color-mix(in srgb, var(--surface-page) 88%, transparent);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--border-subtle);
  }
  .header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-6);
    min-height: 68px;
    flex-wrap: wrap;
    padding-block: var(--space-3);
  }
  .header__home { text-decoration: none; color: inherit; }
  .header__nav {
    display: flex;
    align-items: center;
    gap: var(--space-6);
    flex-wrap: wrap;
  }
  .header__link {
    font-size: var(--text-base);
    font-weight: var(--weight-medium);
    color: var(--text-muted);
    text-decoration: none;
    padding-block: var(--space-2);
  }
  .header__link:hover { color: var(--text-strong); }
  .header__link.is-active {
    color: var(--text-strong);
    box-shadow: inset 0 -2px 0 0 var(--accent);
  }
  .header__cta { padding-inline: var(--space-4); }
  @media (max-width: 720px) {
    .header__nav { gap: var(--space-4); }
  }
</style>
